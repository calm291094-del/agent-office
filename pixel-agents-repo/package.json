{
  "name": "pixel-agents-repo",
  "version": "1.0.0",
  "scripts": {
    "start": "node server.js",
    "build:ui": "cd webview-ui && npm install --include=dev && npm run build"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5"
  }
}
